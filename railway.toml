# Railway conservé en fallback, mais le déploiement cible est Render (render.yaml)
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
